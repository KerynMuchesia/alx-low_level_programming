#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
        char X;

        for (X = 0;Xx <= 9; X++)
        {
                printf("x");
        }
        printf("\n");
        return (0);
}
