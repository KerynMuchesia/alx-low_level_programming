#include "main.h"

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int main(int c)
{
    char c;

    if (c >= 'A' && c <= 'Z)
    {
	    return(1);
    }
    else (c >= 'a' && c <= 'z')
    {
	    return (0);
    }
}
